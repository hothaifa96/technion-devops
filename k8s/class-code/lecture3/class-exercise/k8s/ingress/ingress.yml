apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hack-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
             service:
               name: frontend-svc
               port:
                 number: 5173
          